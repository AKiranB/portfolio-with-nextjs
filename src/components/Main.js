import { Stack } from '@chakra-ui/react'

// eslint-disable-next-line react/display-name
export const Main = (props) => (
  <Stack
    spacing="1rem"
    width="100%"
    height='auto'
    mt="0vh"
    px="1rem"
    {...props}
  />
)
